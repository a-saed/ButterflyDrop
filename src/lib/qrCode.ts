/**
 * Generate QR code data URL for session
 */
export function generateQRCodeData(sessionUrl: string): string {
  // The QR code component will handle the actual generation
  // This is just a placeholder utility if needed
  return sessionUrl
}

